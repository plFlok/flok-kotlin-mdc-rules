---
globs: *.kt,*.java
alwaysApply: false
---
Do not hardcode paths, timings, or URLs.
Move them to the application.yaml configuration file.
Read them from a @Configuration class.

If more than one property is used for domain configuration, create a file
{domain}/configuration/properties/{ConfigurationProperty}.kt
and use the @ConfigurationProperties annotation.