---
globs: *.kt,*.java
alwaysApply: false
---

# Interface first
An interface must be created for every Service class, and the Service must implement this interface.  
An interface must be created for every DAO class, and the DAO must implement its interface.
